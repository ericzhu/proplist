<%= form_for(@property_image, html: {multipart: true}) do |f| %>
  <% if @property_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@property_image.errors.count, "error") %> prohibited this property_image from being saved:</h2>

      <ul>
      <% @property_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image, multiple: true, name: 'property_image[image]' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
