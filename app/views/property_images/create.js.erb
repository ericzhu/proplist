<% if @property_image.new_record?%>
alert("Failed to upload image: <%= j @property_image.errors.full_messages.join(', ').html_safe  %>")
<% else %>
$("#property_images").append("<%= j render(@property_image) %>");
<% end %>